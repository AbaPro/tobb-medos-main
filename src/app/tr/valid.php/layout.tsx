export const metadata = {
  title: 'TOBB MEDOS - Certificate Verification',
  description: 'Certificate verification system for TOBB MEDOS',
}

export default function CertificateLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <>
      {children}
    </>
  )
}
